const {
  mock
} = require('mockjs')
const _ = require('lodash')

class Body {
  constructor(doc, options) {
    this.doc = doc
    this.options = options
  }

  find(path) {

  }

  method(method) {

  }

  mock(data) {

  }
}
